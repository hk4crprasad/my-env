# ──────────────────────────────────────────────────────────────────────────
#  Email Triage RL Environment — runtime requirements
# ──────────────────────────────────────────────────────────────────────────
#  torch is installed separately in the Dockerfile from the cu121 wheel index
#  (T4-compatible). For local CPU-only use, install torch yourself:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
# ──────────────────────────────────────────────────────────────────────────

# ── OpenEnv server (FastAPI + WebSocket) ──────────────────────────────
fastapi
uvicorn[standard]
pydantic
websockets
openenv-core

# ── Inference + adapter loading (Gradio "Baseline vs Trained" tab) ────
transformers
peft
accelerate
bitsandbytes
huggingface_hub
safetensors
sentencepiece

# ── Demo UI ────────────────────────────────────────────────────────────
gradio

# ── Plotting / W&B (optional but referenced by judge_eval + plots) ────
matplotlib
wandb

# ── Persistence (optional — falls back to in-memory if MongoDB absent) ─
motor

# ── HTTP clients ───────────────────────────────────────────────────────
openai
requests
